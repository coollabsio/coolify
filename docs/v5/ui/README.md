# Archived V5 UI Prototype

This directory preserves the previous V5 UI source for design and implementation
reference only. The `.txt` suffixes keep the files outside frontend build and
source-discovery paths.

Do not import or serve these files. V5 will use a different implementation.
